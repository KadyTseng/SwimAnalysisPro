SwimAnalysisPro/
├── main.py                     # 應用程式主入口：啟動 UI，整合前後端。
│
├── BD/                    # 後端分析與處理層：核心分析與數據處理邏輯
│   ├── __init__.py             # Backend 模組初始化
│   ├── orchestrator.py         # 後端協調器 / 主控邏輯：管理分析流程，調度各模組(要把各功能寫成可調用的module)。
│   ├── data_manager.py         # 狀態與進度管理：負責追蹤每個背景分析任務的進度、狀態、錯誤訊息及最終路徑，供前端輪詢查詢。
│   ├── pose_estimator.py       # 骨架模型(models)：負責影片轉關節點 TXT。
│   ├── txt_base.py             # 關節點TXT基本處理(keypoints)：負責把關節點 TXT做內插平滑。
│   ├── diving_analyzer_track_angles.py  # 潛泳軌跡與踢腿角度計算：處理潛泳數據。
│   ├── stroke_style_recognizer.py # 泳姿辨識：判斷泳姿類型。
│   │ 
│   ├── freestyle_pose_estimator/     # 自由式辨識：自由式獨立的骨架。
│   │   ├── freestyle_pose_estimator.py   # 自由式骨架模型(models)：負責影片轉關節點 TXT。
│   │   └── txt_base.py                   # 關節點TXT基本處理(keypoints)：負責把關節點 TXT做內插平滑。   
│   │   
│   ├── stroke_analysis/         # 划手階段演算法庫：根據泳姿選擇不同演算法。
│   │   ├── __init__.py
│   │   ├── backstroke_butterfly_freestyle_stroke_stage.py # 仰式、蝶式、自由式划手分析階段
│   │   ├── backstroke_butterfly_freestyle_stroke_phase_plot.py # 仰式、蝶式、自由式划手畫圖
│   │   ├── breaststroke_stroke_stage.py # 蛙式划手分析階段
│   │   └── breaststroke_stroke_phase_plot.py # 蛙式划手畫圖
│   │   
│   ├── stroke_counter/         # 划手次數演算法庫：根據泳姿選擇不同演算法。
│   │   ├── __init__.py
│   │   ├── backstroke_butterfly_freestyle_stroke_counter.py # 仰式、自由式、蝶式划手次數
│   │   └── breaststroke_counter.py # 蛙式划手次數
│   │   
│   ├── split_speed_analyzer.py # 分段計時與速度計算：計時。
│   ├── focus_tracking_view.py # 追焦框：追焦畫面剪裁。
│   └──video_postprocessor.py  # 後製影片生成：將所有分析數據疊加到影片。
│
├── data/                       # 數據儲存層：專案運行時生成的數據
│   ├── videos/                 # 可選：存放原始影片的副本（如果需要）。
│   ├── keypoints/              # 存放骨架模型生成的關節點 TXT 檔案。
│   ├── models/                 # 存放模型(全泳姿、單獨自由式、SVM)。
│   └── processed_videos/       # 存放最終生成的後製影片。
│ 
├── .gitignore                  # Git 版本控制忽略文件。
├── requirements.txt          # 專案所需的 Python 函式庫列表。
└── Project_arc.txt             # 專案架構。